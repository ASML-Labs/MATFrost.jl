name: Run Tests

on:
  workflow_dispatch:
    inputs:
      debug_enabled:
        type: boolean
        description: 'Run the build with tmate debugging enabled (https://github.com/marketplace/actions/debugging-with-tmate)'
        required: false
        default: false
jobs:
  test:
    runs-on: ubuntu-20.04 # 20.04 contains the last version of gcc that is compatible with MATLAB R2022b
    strategy:
      matrix:
        julia-version: ['1.10']
        matlab-version: [R2022b]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: julia-actions/install-juliaup@v2
        with:
          channel: ${{ matrix.julia-version }}
          
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}

      - name: Set up MATLAB
        uses: matlab-actions/setup-matlab@v2
        with:
          release: ${{ matrix.matlab-version }}
          cache: true
        
      - name: Run tests
        uses: matlab-actions/run-tests@v2
        env:
          JULIA_VERSION: ${{ matrix.julia-version }}
        with:
          source-folder: 'src/matlab'
          select-by-folder: 'test'
          strict: false
          test-results-junit: test-reults/results.xml
          code-coverage-cobertura: test-reults/coverage.xml

      - uses: 5monkeys/cobertura-action@master
        if: always()
        with:
          path: test-reults/coverage.xml
          fail_below_threshold: false
          minimum_coverage: 69

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with: 
          path: test-reults
